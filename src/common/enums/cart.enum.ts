export enum CouponDiscountType {
    PERCENTAGE = 'percentage',
    FIXED = 'fixed',
    BOGO = 'bogo' // Buy One Get One Free
}

export enum CouponRedeemedStatus {
    PENDING = 'pending',
    REDEEMED = 'redeemed',
    EXPIRED = 'expired'
}
